---
status: ACTIVE
---
# CEO Plan: RepoMind v1 Scaffold
Generated by /plan-ceo-review on 2026-06-21
Branch: main | Mode: SCOPE EXPANSION
Repo: repo-mind (local)

## Vision

### 10x Check
RepoMind becomes the default OSS layer for project knowledge: `npx repo-mind init` +
`repo-mind setup` → agent instantly queries glossary/ADRs/open questions. Knowledge
travels with `git clone`. README hero stat from falsifiable A/B demo.

### Platonic Ideal
Agents query the repo instead of ingesting markdown dumps. Humans merge agent proposals
via git-native `create_draft`. "Context7 for your own repo's institutional knowledge."

## Implementation Approach

**A — Full Approach A as approved design doc** (scaffold → MCP → check/export → A/B → create_draft if P2 passes).

User declined kill-switch-first (C); building full package per design doc.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | `repo-mind setup` (auto MCP + CLAUDE.md snippet for Cursor/Claude) | S | ACCEPTED | Closes P4 distribution gap; design had Cursor snippet TBD |
| 2 | `repo-mind try` + built-in demo corpus | M | DEFERRED | User chose init templates only; A/B corpus seeded in step 6a |
| 3 | GitHub Action `repo-mind/check` | S | DEFERRED v1.1 | Document manual `npx repo-mind check` in CI for v1 |
| 4 | Game-design template pack (new types) | M | SKIPPED | Generic types only per P5 |
| 5 | One example game-doc in `init` (existing type) | S | ACCEPTED | Shows extensibility from idea.md without schema expansion |
| 6 | gstack bridge (read `.project-knowledge/`) | M | SKIPPED | gstack is dev tooling only, not part of end product |
| 7 | `llms.txt` + `docs-index.json` export | S | DEFERRED v1.1 | v1 ships `agents.md` only per design Open Questions #3 |

## Accepted Scope (added to v1)

- Everything in approved design doc Approach A
- **`repo-mind setup`** — Cursor + Claude Code MCP config + CLAUDE.md steering snippet
- **One example game-themed doc** in `init` (uses existing `feature-spec` or `glossary-term` type)

## Deferred to v1.1 / Roadmap

- `repo-mind try` + bundled demo corpus
- GitHub Action for `check`
- `llms.txt`, `docs-index.json` export formats
- gstack bridge (not applicable — gstack is dev-only)
- Game-design vertical (new types)
- SQLite index (Approach B)
- `create_draft` (only after P2 kill-switch passes)

## A/B Test Revision

Design doc arm (c) was "builder's gstack setup". **Revised:** arm (c) = `docs/` folder +
well-authored `AGENTS.md` + `CLAUDE.md` (strong markdown baseline, not gstack).

## Reviewer Concerns (CEO 11-section highlights)

1. **P2 still load-bearing** — ship A/B harness before celebrating; thesis may fail at small corpus.
2. **Slug traversal (A1)** — dedicated adversarial test suite mandatory before any release.
3. **`create_draft` worktree lifecycle** — SIGTERM mid-draft must not orphan worktrees.
4. **Cursor MCP config formats** — `setup` must handle config path drift across Cursor versions.
5. **No git remote yet** — first commit + GitHub repo before npm publish path.
6. **Independent A/B baseline** — second person authors half the questions and markdown baseline.
